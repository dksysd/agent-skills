# 실험 정리: MSDK Git 상태 점검

## 1. 요약
- 한 줄 결론: 두 MSDK 저장소에서 대량 삭제 상태가 확인됐고, 두 AI8X 저장소는 변경 없음 상태였다.
- 상태: 완료
- 핵심 결과: `official_msdk_source_20260611`은 총 `34,147`개 변경, `third_party/msdk`는 총 `32,453`개 변경이 확인됐다.
- 다음 결정: 삭제가 의도된 작업인지 확인하기 전까지 두 MSDK 저장소를 복구하거나 정리하지 않는다.

## 2. 목적
- 왜 이 실험을 했는가: 현재 서버에 남아 있는 Git 변경 흔적을 정리하고, 어떤 저장소에 영향이 있는지 구분하기 위해 수행했다.
- 확인하려는 가설: 대량 변경은 모든 관련 저장소에 퍼진 것이 아니라 일부 MSDK 저장소에 한정됐을 가능성이 있다.
- 성공 기준: 각 저장소의 변경 개수, 상태 종류, 마지막 커밋, 주요 삭제 경로를 명확히 확인한다.

## 3. 기준 상태
- 날짜: `2026-06-13`
- 저장소: `/root/project/korean_denoiser_v1/validation/official_msdk_source_20260611`
- 브랜치: `main`
- 커밋: `3e3bfeec`, `2026-06-10 17:17:18 +0300`
- 실행 환경: `/root` 작업공간, 로컬 Git 상태 기준
- 데이터셋 / 입력 경로: 해당 없음
- 모델 / 체크포인트: 해당 없음
- 비교 기준선: `git status --short`가 비어 있는 clean 상태

## 4. 방법
- 실행 명령:

```bash
git -C <repo> rev-parse --abbrev-ref HEAD
git -C <repo> log -1 --pretty=format:'%h %ad %s' --date=iso
git -C <repo> status --short
```

- 주요 파라미터: 해당 없음
- 변경한 설정: 없음
- 평가 방식: `git status --short` 출력에서 상태 코드별 개수를 집계했다. `D`는 삭제된 추적 파일, `??`는 Git이 아직 추적하지 않는 파일을 뜻한다.
- 반복 횟수 / seed: 해당 없음

## 5. 결과
- 주요 지표: 삭제 `34,140`개, 미추적 `7`개, 총 변경 `34,147`개
- 생성 산출물: 이 정리 문서
- 로그 위치: 별도 로그 파일 없음
- 실패 / 경고 / 이상 징후: `Examples`, `Libraries`, `Tools`, `Documentation`, `.github` 등 상위 경로의 대량 삭제가 확인됐다.

## 6. 판단
- 결과 해석: 해당 저장소에는 대량 삭제 상태가 존재한다.
- 주장 가능한 것: `2026-06-13` 기준 로컬 Git 상태에서 대량 삭제 상태가 확인됐다.
- 아직 주장하면 안 되는 것: 누가 삭제했는지, 삭제가 의도됐는지, 어떤 명령으로 삭제됐는지는 이 증거만으로 확정할 수 없다.
- 리스크: 무심코 `git add`, `git commit`, `git clean`, `git reset`을 실행하면 현재 증거 상태가 바뀔 수 있다.

## 7. 후속 작업
- 다음 실험: 삭제 파일 목록과 미추적 파일 목록을 별도 파일로 보존한다.
- 보완할 데이터 / 코드: 필요 시 shell history, 작업 로그, 파일 수정 시각을 추가로 확인한다.
- 정리 / 삭제 / 보존할 산출물: 원인 파악 전까지 현재 상태를 보존한다.
